Problem: Find if singly linked list is palindrome

Solution:

1. slow/fast pointer to find the middle of the list
2. reverse second part of list
3. compare both list

Time complexity: O(n)
Space complexity: O(1)