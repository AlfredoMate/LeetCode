Problem: Check given missing for missing numbers in range (1, len(nums))

Solution:

Time: O(N): Iterate through list to convert to set and iterate through list range which num is missing from list.
Space: O(N). 

Note: There is a solution with constant space.