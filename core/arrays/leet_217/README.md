Solution:

Time and Space: O(N) Iterate through list and create hashSet of size of the list (worst case).


HashSets (sets) are fast
Implemented as a hash table. So you can expect a lookup/insert/delete in 0(1) average.