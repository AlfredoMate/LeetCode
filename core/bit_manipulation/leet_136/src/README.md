Problem: Find only unique number

Solution:

XOR is commutative, duplicates equal 0 (all bits are cancelled) and XOR with 0 has no effect.
We use XOR on all the array, linear time and constant space complexity.