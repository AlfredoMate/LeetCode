Problem: Evaluate reverse polish notation

Solution: We iterate. If number push into stack. If operator, pop two opearnds from stack, perform operation and push result into stack.

Time complexity : O(n)
Space complexity: O(n)