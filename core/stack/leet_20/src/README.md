Problem: Valid Parenthesis

Solution:

We push open parenthesis to stack. We check for every closig parenthesis if it's corresponding opening is the top of the stack.

Time complexity: O(n)

Space complexity: O(n)