problem: find min absolute difference and return pairs that equal that difference

solution:

sort the array
iterate array to find min distance
iterate again and return the pairs that satisfy that distance