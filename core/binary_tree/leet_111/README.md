Problem: min depth of binary tree

Solution: 

We use breath first search logic (queue no recursive). When we find a leaf (both child nodes are null) we return.

Time complexity: O(n)

Space complexity: O(n) 

Worst case scenario: completely skewed tree

