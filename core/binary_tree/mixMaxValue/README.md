Problem: Find max/min of a given binary tree

Solution:

We traverse all nodes of the tree. We can fdo it both using breadth first search or depth first search.

Time complexity: bfs O(n) dfs O(n)
Space complexity: bfs O(n) Stack structure dfs O(n) function call stack
