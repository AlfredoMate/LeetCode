Problem: fibo sequence

Solution:

We apply recursion while saving in a set all previously computed results so that fibo is only calculated once.

Linear space (recursion stack + hashmap) and time complexity (we compute once each number)