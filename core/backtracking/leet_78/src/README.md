Problem: from a given array, return all possible subsets

Solution:

DFS with backtracking once we return from a lower leaf. 
Time complexity: 2^n * n (creating new ArrayList)
Space complexity: O(n) (recursion)